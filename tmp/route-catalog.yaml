apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: default-catalog-gateway
  namespace: istio-system
spec:
  host: webapp.istioinaction.io
  port:
    targetPort: http2
  to:
    kind: Service
    name: istio-ingressgateway
    weight: 100
  wildcardPolicy: None
